<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Sample form view -->
    <record id="laptop_view_form" model="ir.ui.view">
        <field name="name">laptop.view.form</field>
        <field name="model">laptop</field>
        <field name="arch" type="xml">
            <form string="Sample">
                <header>
                    <button name="button_action" string="Submit" type="object" class="btn-primary"/>
                    <field name="state" widget="statusbar" options="{'clickable': '1'}"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <label for="name"/>
                        <h1>
                            <field name="name" placeholder="e.g Marc Demo"/>
                        </h1>
                    </div>
                    <group>
                        <field name="active" invisible="1"/>
                    </group>
                    <notebook>
                        <page string="Details">
                            <field name="description"/>
                        </page>
                    </notebook>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <!-- Sample list view -->
    <record id="laptop_view_list" model="ir.ui.view">
        <field name="name">laptop.view.list</field>
        <field name="model">laptop</field>
        <field name="arch" type="xml">
            <list string="Samples">
                <field name="name"/>
                <field name="state"/>
            </list>
        </field>
    </record>

    <!-- Kanban View -->
    <record id="laptop_view_kanban" model="ir.ui.view">
        <field name="name">laptop.view.kanban</field>
        <field name="model">laptop</field>
        <field name="arch" type="xml">
            <kanban>
                <field name="state"/>
                <templates>
                    <t t-name="kanban-box">
                        <div class="oe_kanban_global_click">
                            <field name="name"/>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

 
</odoo>